# database/ 定时任务脚本依赖（独立于 backend）
# 安装: python -m venv .venv && .venv/bin/pip install -r requirements.txt
baostock>=0.9.1
pymysql>=1.1.0
yfinance 
akshare 
pandas 
numpy 
matplotlib 

# quant/ 分析系统依赖
pymysql>=1.1.0
plotly>=5.20
streamlit>=1.33
pytest>=8.0